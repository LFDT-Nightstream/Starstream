# E0041: Return Type Not Allowed

A UTXO `main fn` was declared with a return type.

UTXO `main fn`s always have an automatically implied return type.

## How to fix

Remove the return type annotation from the `main fn`.

```starstream
utxo {
    main fn new() {
        // ...
    }
}
```
