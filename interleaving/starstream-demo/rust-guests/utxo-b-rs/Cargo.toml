[package]
name = "utxo-b-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
dlmalloc = { version = "0.2.13", features = ["global"] }
wit-bindgen-rt = { version = "0.44.0", features = ["bitflags"] }

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "starstream-demo:utxo-b-rs"

[package.metadata.component.dependencies]
